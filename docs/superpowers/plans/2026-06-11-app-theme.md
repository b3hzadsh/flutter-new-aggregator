# App Theme (RTL + Material 3) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement a Material 3 theme with RTL support and Vazirmatn typography for the Iranian News Aggregator.

**Architecture:** Centralized theme definition in `AppTheme` class and global application of RTL settings in `MaterialApp`.

**Tech Stack:** Flutter, Material 3, `flutter_localizations`.

---

### Task 1: Add Dependencies

**Files:**
- Modify: `pubspec.yaml`

- [ ] **Step 1: Add `flutter_localizations` to dependencies**

```yaml
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations: # Add this
    sdk: flutter
  cupertino_icons: ^1.0.8
  # ... rest of dependencies
```

- [ ] **Step 2: Run `flutter pub get`**

Run: `flutter pub get`
Expected: PASS

- [ ] **Step 3: Commit**

```bash
git add pubspec.yaml
git commit -m "chore: add flutter_localizations dependency"
```

---

### Task 2: Create App Theme

**Files:**
- Create: `lib/presentation/theme/app_theme.dart`

- [ ] **Step 1: Implement `AppTheme` class**

```dart
import 'package:flutter/material.dart';

class AppTheme {
  static ThemeData get lightTheme => ThemeData(
    useMaterial3: true,
    colorScheme: ColorScheme.fromSeed(
      seedColor: Colors.teal,
      brightness: Brightness.light,
    ),
    fontFamily: 'Vazirmatn',
    textTheme: const TextTheme(
      bodyLarge: TextStyle(height: 1.6),
      bodyMedium: TextStyle(height: 1.6),
      titleLarge: TextStyle(fontWeight: FontWeight.bold),
    ),
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add lib/presentation/theme/app_theme.dart
git commit -m "feat: implement AppTheme with Material 3 and Vazirmatn"
```

---

### Task 3: Apply Theme and RTL Globally

**Files:**
- Modify: `lib/main.dart`

- [ ] **Step 1: Update `MyApp` with theme and localization settings**

```dart
import 'package:flutter/material.dart';
import 'package:flutter_localizations/flutter_localizations.dart';
import 'package:news_aggregator/presentation/theme/app_theme.dart';

void main() {
  runApp(const MyApp());
}

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Iranian News Aggregator',
      theme: AppTheme.lightTheme,
      localizationsDelegates: const [
        GlobalMaterialLocalizations.delegate,
        GlobalWidgetsLocalizations.delegate,
        GlobalCupertinoLocalizations.delegate,
      ],
      supportedLocales: const [
        Locale('fa', 'IR'),
      ],
      locale: const Locale('fa', 'IR'),
      home: const MyHomePage(title: 'خبرخوان ایرانی'),
    );
  }
}
// ... rest of the file (MyHomePage and _MyHomePageState)
```

- [ ] **Step 2: Commit**

```bash
git add lib/main.dart
git commit -m "feat: apply AppTheme and global RTL settings"
```

---

### Task 4: Verification

- [ ] **Step 1: Run `flutter analyze`**

Run: `flutter analyze`
Expected: No issues found.

- [ ] **Step 2: Run `flutter test`**

Run: `flutter test`
Expected: All tests pass (though current tests are for the counter app, they should still pass).
